{
  "name": "synthesis-coding-site",
  "version": "1.0.0",
  "private": true,
  "description": "Synthesis Coding methodology website with dual publishing to synthesiscoding.com and rajiv.com",
  "scripts": {
    "build": "node scripts/build.js",
    "build:watch": "node scripts/build.js --watch",
    "build:wordpress": "node scripts/build.js --wordpress-only"
  },
  "devDependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^15.0.4"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
